import React from "react";

import {
  Dialog,
  DialogTitle,
  DialogContent,
  DialogActions,
} from "@material-ui/core";
import CloseIcon from "@material-ui/icons/Close";

const Modal = ({ open, setOpen, filtr, setFiltr }) => {
  return <Dialog open={open}>Ahoj</Dialog>;
};

export default Modal;
